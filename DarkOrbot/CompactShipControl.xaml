<Window x:Class="DarkOrbot.CompactShipControl"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
         xmlns:obj="clr-namespace:DarksideApi.DarkOrbit;assembly=DarksideApi"
         xmlns:hlp="clr-namespace:DarkOrbot.Util"
         xmlns:stats="clr-namespace:DarksideApi.Statistics;assembly=DarksideApi"
         xmlns:cmds="clr-namespace:DarksideApi.DarkOrbit.Commands;assembly=DarksideApi"
         xmlns:settings="clr-namespace:DarksideApi.DarkOrbit.Settings;assembly=DarksideApi"
         xmlns:local="clr-namespace:DarkOrbot"
         mc:Ignorable="d" Height="360" Width="220" IsVisibleChanged="Window_IsVisibleChanged" Loaded="Window_Loaded" Unloaded="Window_Loaded"  AllowsTransparency="True" Background="#1d1d1f"
DataContext="{Binding RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" WindowStyle="None" ShowInTaskbar="False">
    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Background" Value="#FF040406" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style TargetType="Button" x:Name="RoundedCornerBtn" x:Key="RoundedCornerBtn">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid>
                            <Border Name="Border" Margin="0,0,0,0" Background="Black"
BorderBrush="White" BorderThickness="1,1,1,1" CornerRadius="5">
                                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center"
            HorizontalAlignment="Center"
            ContentSource="Header" Margin="12,2,12,2"
            RecognizesAccessKey="True">
                                </ContentPresenter>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Panel.ZIndex" Value="100" />
                                <Setter TargetName="Border" Property="Background" Value="Gray" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,1" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="Gray" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style BasedOn="{StaticResource {x:Type DataGridColumnHeader}}" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" >
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black" Offset="0.9"/>
                        <GradientStop Color="#FF030304" Offset="1"/>
                        <GradientStop Color="Black" Offset="0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarLineButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Visibility" Value="Hidden"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Name="Border" Margin="1" CornerRadius="2" Background="#7F040406" BorderBrush="Gray" BorderThickness="1">
                            <Path HorizontalAlignment="Center" VerticalAlignment="Center" Fill="White" Data="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="WhiteSmoke" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="DarkGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Visibility" Value="Hidden"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="DimGray" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0"  Width="8" Margin="8,0,-2,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition MaxHeight="0"/>
                    <RowDefinition Height="0.00001*"/>
                    <RowDefinition MaxHeight="0"/>
                </Grid.RowDefinitions>
                <Border Grid.RowSpan="3" CornerRadius="2" Background="Transparent" />
                <RepeatButton Grid.Row="0" Style="{StaticResource ScrollBarLineButton}" Height="18" Command="ScrollBar.LineUpCommand" Content="M 0 4 L 8 4 L 4 0 Z" />
                <Track Name="PART_Track" Grid.Row="1" IsDirectionReversed="true">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumb}" Margin="1,0,1,0" Background="#1b82e3" BorderBrush="Gray" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Grid.Row="3" Style="{StaticResource ScrollBarLineButton}" Height="18" Command="ScrollBar.LineDownCommand" Content="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>
        <ControlTemplate x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="18"/>
                    <ColumnDefinition Width="0.00001*"/>
                    <ColumnDefinition MaxWidth="18"/>
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="3" CornerRadius="2" Background="Transparent"/>
                <RepeatButton Grid.Column="0"  Style="{StaticResource ScrollBarLineButton}" Width="18" Command="ScrollBar.LineLeftCommand" Content="M 4 0 L 4 8 L 0 4 Z" />
                <Track Name="PART_Track" Grid.Column="1" IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumb}" Margin="0,1,0,1" Background="#1b82e3" BorderBrush="Gray" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
                <RepeatButton Grid.Column="3" Style="{StaticResource ScrollBarLineButton}" Width="18" Command="ScrollBar.LineRightCommand" Content="M 0 0 L 4 4 L 0 8 Z"/>
            </Grid>
        </ControlTemplate>
        <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="12" />
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                </Trigger>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="18"/>
                    <Setter Property="Height" Value="Auto" />
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FavsScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="1"/>
                            <ScrollBar Name="PART_VerticalScrollBar" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar Name="PART_HorizontalScrollBar" Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#FF040406" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Opacity" Value="75" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="Gray" Opacity="{TemplateBinding Opacity}" BorderThickness="0.5" CornerRadius="3">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle">
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="#FF040406" />
            <Setter Property="BorderThickness" Value="0.5" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Grid SnapsToDevicePixels="true">
                            <Border BorderBrush="Gray" BorderThickness="0.5" CornerRadius="3">

                                <DockPanel>
                                    <ContentPresenter x:Name="Icon" ContentSource="Icon" Margin="4,0,6,0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" />
                                    <Path x:Name="GlyphPanel" Fill="{TemplateBinding Foreground}" FlowDirection="LeftToRight" Margin="7,0,0,0" Visibility="Collapsed" VerticalAlignment="Center" />
                                    <ContentPresenter x:Name="content" ContentSource="Header" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </DockPanel>
                            </Border>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Focusable="false" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" Placement="Bottom" VerticalOffset="-1">
                                <Border BorderThickness="0.5" BorderBrush="Gray" Background="#FF040406">
                                    <ScrollViewer x:Name="SubMenuScrollViewer" CanContentScroll="true" Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <Grid RenderOptions.ClearTypeHint="Enabled">
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle" Grid.IsSharedSizeScope="true" Margin="2" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.TabNavigation="Cycle" />
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsChecked" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="TextBlock.Foreground" Value="Green" TargetName="content" />
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="TextBlock.Foreground" Value="LightGray" TargetName="content" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="TextBlock.Foreground" Value="SpringGreen" TargetName="content" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border BorderBrush="#1b82e3" BorderThickness="0.5">
        <Grid>

            <Button x:Name="closeBtn" Style="{StaticResource RoundedCornerBtn}" BorderThickness="1" BorderBrush="White" HorizontalAlignment="Right" Height="25" VerticalAlignment="Top" Width="25" Foreground="{x:Null}" Click="closeBtn_Click" ToolTip="Close" Margin="0,5,5,0">
                <Button.Background>
                    <ImageBrush ImageSource="/Icons/closeIco.png"/>
                </Button.Background>
            </Button>

            <Image x:Name="shipImage" HorizontalAlignment="Left" Height="120" Margin="10,10,0,0" VerticalAlignment="Top" Width="120"/>
            <Label x:Name="shipUsernameDescLabel" Content="Username:" HorizontalAlignment="Left" Foreground="White" Margin="10,148,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipUsernameLabel" Content="{Binding Api.Hero.Username}" d:Content="/" HorizontalAlignment="Left" Foreground="White" Margin="81,148,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipClanDescLabel" Content="Clan:" HorizontalAlignment="Left" Foreground="White" Margin="10,173,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipClanLabel" Content="{Binding Api.Hero.ClanTag}" d:Content="/" HorizontalAlignment="Left" Foreground="White" Margin="81,173,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipUserIdDescLabel" Content="UserId:" HorizontalAlignment="Left" Foreground="White" Margin="10,196,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipUserIdLabel" Content="{Binding Api.Hero.UserId}" d:Content="/" HorizontalAlignment="Left" Foreground="White" Margin="81,196,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipPremiumDescLabel" Content="Premium:" HorizontalAlignment="Left" Foreground="White" Margin="10,217,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipPremiumLabel" Content="{Binding Api.Hero.HasPremium}" d:Content="False" HorizontalAlignment="Left" Foreground="White" Margin="81,217,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipFormattionDescLabel" Content="Formattion:" HorizontalAlignment="Left" Foreground="White" Margin="10,259,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipAvgDamageLabel" Content="{Binding Api.Statistics.AvgDamage.AmountString}" d:Content="42" HorizontalAlignment="Left" Foreground="White" Margin="81,301,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipAvgClicksDescLabel" Content="Avg. Clicks:" HorizontalAlignment="Left" Foreground="White" Margin="10,280,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipAvgSearchTimeLabel" Content="{Binding Api.Statistics.AvgSearchTime.AmountString}" d:Content="42" HorizontalAlignment="Left" Foreground="White" Margin="81,322,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipConfigDescLabel" Content="Config:" HorizontalAlignment="Left" Foreground="White" Margin="10,238,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipAvgClicksLabel" d:Content="42" Content="{Binding Api.Statistics.AvgClicks.AmountString}" HorizontalAlignment="Left" Foreground="White" Margin="81,280,0,0" VerticalAlignment="Top"/>

            <Label x:Name="shipCurrentLaserLabel" Content="{Binding Api.Hero.CurrentLaser}" d:Content="LCB-10" HorizontalAlignment="Left" Foreground="White" Margin="140,42,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipCurrentRocketLabel" Content="{Binding Api.Hero.CurrentRocket}" d:Content="ROCKET" HorizontalAlignment="Left" Foreground="White" Margin="140,73,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipCurrentRocketLauncherLabel" Content="{Binding Api.Hero.CurrentRocket}" d:Content="ROCKETLAUNCHER" HorizontalAlignment="Left" Foreground="White" Margin="140,104,0,0" VerticalAlignment="Top"/>
            <ProgressBar x:Name="shipCargoCapacityProgressBar" BorderThickness="0" Foreground="Gold" HorizontalAlignment="Left" Height="10" Margin="10,134,0,0" VerticalAlignment="Top" Width="125" Maximum="{Binding Api.Hero.Cargo.MaxCargoSpace}" Minimum="0" Value="{Binding Api.Hero.Cargo.UsedCargoSpace}" d:Value="90" d:Maximum="100">
                <ProgressBar.ToolTip>
                    <ToolTip x:Name="shipCargoCapacityProgressBarToolTip" Content="{Binding Api.Hero.Cargo.UsedCargoSpace}"/>
                </ProgressBar.ToolTip>
            </ProgressBar>
            <Label x:Name="shipCurrentLaserAmountLabel" Content="{Binding Api.Hero.CurrentLaser.Amount}" d:Content="999999" HorizontalAlignment="Left" Foreground="White" Margin="140,57,0,0" VerticalAlignment="Top" FontSize="10"/>
            <Label x:Name="shipCurrentRocketAmountLabel" Content="{Binding Api.Hero.CurrentRocket.Amount}" d:Content="999999" HorizontalAlignment="Left" Foreground="White" Margin="140,88,0,0" VerticalAlignment="Top" FontSize="10"/>
            <Label x:Name="shipCurrentRocketLauncherAmountLabel" Content="{Binding Api.Hero.CurrentRocketLauncher.Amount}" d:Content="9999" HorizontalAlignment="Left" Foreground="White" Margin="140,121,0,0" VerticalAlignment="Top" FontSize="10"/>
            <Label x:Name="shipAvgDamageDescLabel" Content="Avg. Dmg:" HorizontalAlignment="Left" Foreground="White" Margin="10,301,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipAvgSearchTimeDescLabel" Content="Avg. Search:" HorizontalAlignment="Left" Foreground="White" Margin="10,322,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipConfigLabel" Content="{Binding Api.Hero.Config}" d:Content="/" HorizontalAlignment="Left" Foreground="White" Margin="81,238,0,0" VerticalAlignment="Top"/>
            <Label x:Name="shipFormattionLabel" Content="{Binding Api.Hero.DroneFormationReadable}" d:Content="/" HorizontalAlignment="Left" Foreground="White" Margin="81,259,0,0" VerticalAlignment="Top"/>

        </Grid>
    </Border>
</Window>
