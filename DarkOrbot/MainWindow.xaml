<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DarkOrbot" x:Name="darkOrbotMainWindow"
        x:Class="DarkOrbot.MainWindow"
        mc:Ignorable="d"
        Title="Darkside" Height="380" Width="560" MaxWidth="560" Loaded="Window_Loaded" Closing="Window_Closing" Background="#1d1d1f" LocationChanged="darkOrbotMainWindow_LocationChanged" SizeChanged="darkOrbotMainWindow_SizeChanged" SizeToContent="Height">
    <Window.Resources>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Background" Value="#FF040406" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid>
                            <Border Name="Border" Margin="0,0,0,0" Background="Black"
BorderBrush="White" BorderThickness="1,1,1,1" CornerRadius="5">
                                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center"
            HorizontalAlignment="Center"
            ContentSource="Header" Margin="12,2,12,2"
            RecognizesAccessKey="True" MinHeight="16">
                                </ContentPresenter>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Panel.ZIndex" Value="100" />
                                <Setter TargetName="Border" Property="Background" Value="#1d1d1f" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,1" />
                                <Setter Property="Foreground" Value="#1b82e3" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <TabControl x:Name="mainTabControl" Background="Transparent" Visibility="Visible" Panel.ZIndex="99" SelectionChanged="mainTabControl_SelectionChanged" VerticalAlignment="Stretch">
    </TabControl>
</Window>
